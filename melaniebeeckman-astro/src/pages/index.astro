---
import Layout from '@layouts/Layout.astro';
import { getAsLanguage } from '@i18n/utils';
import IndexComponent from '@components/IndexComponent.astro';

const lang = getAsLanguage(Astro.params.lang, Astro.currentLocale);

---

<Layout lang={lang}>
    <IndexComponent  lang={lang} />
</Layout>